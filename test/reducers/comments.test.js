require('../support')
import comments from '../../src/reducers/comments'
import {
  FETCH_COMMENTS,
  CREATE_COMMENT
} from '../../src/actions'

describe('comments', () => {
  it('passes', () => {
    expect(true)
  })
})
